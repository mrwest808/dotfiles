#!/bin/bash
set -e

DIR="$( cd "$( dirname "${BASH_SOURCE=[0]}" )" && pwd )"

# Install oh-my-fish
curl -L https://get.oh-my.fish | fish

# TODO Symlink config files (or copy, had some issues when trying symlinks)
